/** The five-part port: p(name, dataType, size/capacity, storage/landing, why) */
export const p = (n, t, l, s, d) => ({ n, t, l, s, d });

/** A rectangle. `k` is one of person/ext/app/extc/lib/comp/code/store/risk/note */
export const blk = o => o;
